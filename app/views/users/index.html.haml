%h1 Listing users

%table.table
  %tr
    %th Username
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.username
      %td= link_to 'Show', user, class: 'btn'
      %td= link_to 'Edit', edit_user_path(user), class: 'btn'
      %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn'

%br

= link_to 'New User', new_user_path, class: 'btn'
